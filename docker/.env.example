# Supabase Configuration (from your existing instance at /opt/supabase-project)
# Get these from: http://64.225.76.110:8000 (Kong gateway) or Supabase Studio
SUPABASE_URL=http://supabase-kong:8000
SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# OpenAI API (for Whisper transcription)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Security Keys
# Generate with: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_minimum_32_characters_here
ENCRYPTION_KEY=your_encryption_key_minimum_32_characters_here

# Domain Configuration
# Update this to your actual domain
DOMAIN=xinote.amega.one

# Database Encryption Password (for PGP encrypted patient data)
# Generate with: openssl rand -base64 32
DB_ENCRYPTION_PASSWORD=your_db_encryption_password_here

# Optional: Email notifications (for transcription completion)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=notifications@xinote.com
SMTP_PASSWORD=your_smtp_password
SMTP_FROM=noreply@xinote.com

# Optional: Sentry for error tracking
SENTRY_DSN=
